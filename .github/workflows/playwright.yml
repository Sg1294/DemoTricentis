name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    env:
      TEST_USER_EMAIL: ${{ secrets.USERNAME }}
      TEST_USER_PASSWORD: ${{ secrets.PASSWORD }}
    defaults:
      run:
        working-directory: ./PlayWright
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Verify test credentials are available
      run: |
        echo "Checking environment variables..."
        echo "TEST_USER_EMAIL length: ${#TEST_USER_EMAIL}"
        echo "TEST_USER_PASSWORD length: ${#TEST_USER_PASSWORD}"
        if [ -z "$TEST_USER_EMAIL" ]; then
          echo "❌ CRITICAL: TEST_USER_EMAIL is EMPTY or NOT SET"
          echo "   This means secrets.USERNAME has no value in GitHub Secrets"
        else
          echo "✅ TEST_USER_EMAIL is set (length: ${#TEST_USER_EMAIL})"
        fi
        if [ -z "$TEST_USER_PASSWORD" ]; then
          echo "❌ CRITICAL: TEST_USER_PASSWORD is EMPTY or NOT SET"
          echo "   This means secrets.PASSWORD has no value in GitHub Secrets"
        else
          echo "✅ TEST_USER_PASSWORD is set (length: ${#TEST_USER_PASSWORD})"
        fi
    - name: Run Playwright tests
      run: npx playwright test
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: PlayWright/playwright-report/
        retention-days: 30
